server:
  port: 8080

spring:
  application:
    name: 02-limits-service
  cloud:
    config:
      uri: http://localhost:8888
#  profiles:
#    active: qa

management:
  #  security:
  #    enabled: false
  endpoints:
    web:
      #      enable all actuator URLs from Spring Boot 2.0.0
      exposure:
        include: "*"
